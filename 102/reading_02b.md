# Git Remotes

* **git remote:** 

```bash
git remote
```
See the shortt names of the handles.

* **git remote -v:**
```bash
git remote -v
```

 View all the remote URLs next to their corresponding short names.